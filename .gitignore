node_modules
dist
dist-standalone
.env
.env.local
tsconfig.tsbuildinfo
.DS_Store
# public/descargas/habitat-piloto.zip SÍ se versiona a propósito: es el
# paquete descargable que el sitio ofrece desde el botón "Descargar app".
.vercel
.env*
!.env.example
.fuse_hidden*
# Carpeta temporal: builds de verificación que no se pueden borrar desde el
# entorno remoto. Es seguro eliminarla a mano.
_to_delete
_respaldo-*
playwright-report
test-results
dist-verificacion-mt
dist-vmt2
