import { Plus } from "lucide-react";
export function ClientsHeader({ hot, unqualified, onCreate }: { hot: number; unqualified: number; onCreate: () => void }) {
  return <div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-xl font-bold text-slate-900 sm:text-2xl">Clientes</h1><p className="mt-1 text-sm text-slate-500">Tu cartera ordenada por qué tan cerca está cada persona de comprar.</p></div><div className="flex flex-wrap items-center gap-2 text-xs"><span className="rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 font-semibold text-emerald-700">{hot} listos para cerrar</span>{unqualified > 0 && <span className="rounded-full border border-amber-200 bg-amber-50 px-3 py-1.5 font-semibold text-amber-700">{unqualified} sin calificar</span>}<button onClick={onCreate} className="flex items-center gap-1.5 rounded-full bg-violet-600 px-4 py-2 text-xs font-semibold text-white shadow-md shadow-violet-300/60 hover:bg-violet-700"><Plus className="size-3.5" /> Agregar cliente</button></div></div>;
}
